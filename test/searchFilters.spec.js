var SearchFilters = require('../server/searchFilters.js');
var expect = require('chai').expect;

describe('SearchFilters', function(){

    it.only('Should accept an express Request object on instantiation', function(done){

    });

    it('Should save the path and other query params');

    it('Should parse the f param and save a list of Filter objects');

    it('Should be able to return a url without a given filter');

    it('Should be able to return a url with a passed filter included');

});